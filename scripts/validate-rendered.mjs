import fs from 'node:fs';
import path from 'node:path';
import process from 'node:process';

const root=process.cwd();const articleRoot=path.join(root,'.next/server/app/articles');const errors=[];
const files=[];const familyCoverage=new Set();const variantCoverage=new Map();const modes=new Set();const signatures=new Map();const walk=(directory)=>{for(const entry of fs.readdirSync(directory,{withFileTypes:true})){const target=path.join(directory,entry.name);if(entry.isDirectory())walk(target);else if(entry.name.endsWith('.html'))files.push(target);}};walk(articleRoot);
if(files.length!==140)errors.push(`expected 140 rendered article pages, found ${files.length}`);
for(const file of files){const html=fs.readFileSync(file,'utf8');const slug=path.basename(file,'.html');const figures=(html.match(/<figure/g)||[]).length;const families=[...html.matchAll(/data-visual-family="([^"]+)"/g)].map(match=>match[1]);const variants=[...html.matchAll(/data-visual-variant="([^"]+)"/g)].map(match=>match[1]);const mode=html.match(/data-publication-mode="([^"]+)"/)?.[1];const text=html.replace(/<script[\s\S]*?<\/script>/g,' ').replace(/<style[\s\S]*?<\/style>/g,' ').replace(/<[^>]+>/g,' ').replace(/&[^;]+;/g,' ');const words=(text.match(/[A-Za-z][A-Za-z'-]*/g)||[]).length;
  if(figures<4||figures>5)errors.push(`${slug}: expected 4–5 rendered figures, found ${figures}`);
  if(families.length!==4)errors.push(`${slug}: expected four design-system visuals, found ${families.length}`);
  if(new Set(families).size<3)errors.push(`${slug}: visual story uses fewer than three families (${families.join(', ')})`);
  if(!mode)errors.push(`${slug}: missing publication mode`);else modes.add(mode);
  families.forEach((family,index)=>{familyCoverage.add(family);const familyVariants=variantCoverage.get(family)||new Set();familyVariants.add(variants[index]);variantCoverage.set(family,familyVariants);});signatures.set(slug,`${mode}|${families.map((family,index)=>`${family}:${variants[index]}`).join('|')}`);
  if(words<800)errors.push(`${slug}: expected at least 800 rendered words, found ${words}`);
  for(const marker of ['Issue','Published','References','Related articles','Article navigation'])if(!html.includes(marker))errors.push(`${slug}: missing rendered ${marker}`);
  if(!html.includes('datePublished'))errors.push(`${slug}: missing rendered datePublished structured data`);
}
const expectedFamilies=['architecture','topology','dashboard','timeline','decision-tree','heatmap','service-blueprint','journey-map','knowledge-graph','aircraft','metrics','infographic','risk','table'];for(const family of expectedFamilies){if(!familyCoverage.has(family))errors.push(`design-system family not rendered: ${family}`);if((variantCoverage.get(family)?.size||0)<3)errors.push(`${family}: expected all three layout variants`);}if(modes.size<8)errors.push(`expected all 8 publication modes, found ${modes.size}`);const duplicateSignatures=new Map();for(const [slug,signature] of signatures){const slugs=duplicateSignatures.get(signature)||[];slugs.push(slug);duplicateSignatures.set(signature,slugs);}for(const [signature,slugs] of duplicateSignatures)if(slugs.length>2)errors.push(`visual identity repeated ${slugs.length} times (${slugs.join(', ')}): ${signature}`);
const sitemapPath=path.join(root,'.next/server/app/sitemap.xml.body');if(!fs.existsSync(sitemapPath))errors.push('rendered sitemap is missing');else{const sitemap=fs.readFileSync(sitemapPath,'utf8');const urls=(sitemap.match(/<url>/g)||[]).length;if(urls!==141)errors.push(`expected 141 sitemap URLs, found ${urls}`);if(!sitemap.includes('/articles/2017-01-technical-record-digitization'))errors.push('January 2017 missing from sitemap');}
if(errors.length){console.error(`Rendered publication validation failed (${errors.length})\n- ${errors.join('\n- ')}`);process.exit(1);}console.log(`Rendered publication validation passed: ${files.length} articles, ${familyCoverage.size} visual families, ${modes.size} publication modes, 4 design-system artifacts per article, substantive text, metadata, navigation, references, and 141 sitemap URLs.`);
