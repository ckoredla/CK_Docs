import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function RiskCanvas(props:FamilyProps){const labels=['Monitor','Control','Escalate','Stop'];return <VisualFrame family="risk" props={props} caption="Likelihood and consequence determine control intensity; uncertainty remains visible as a separate concern."><div className="pv-risk"><div className="pv-risk-grid">{Array.from({length:16},(_,i)=><i key={i} data-risk={Math.floor(i/4)+i%4}/>)}</div><div className="pv-risk-axis"><span>Likelihood →</span><span>Consequence ↑</span></div><div className="pv-risk-labels">{labels.map(x=><b key={x}>{x}</b>)}</div></div></VisualFrame>}
