import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function RiskCanvas(props:FamilyProps){const labels=[['Monitor','Routine observation'],['Control','Engineered guardrail'],['Escalate','Qualified review'],['Stop','Outside authority']];return <VisualFrame family="risk" props={props} caption="Likelihood and consequence determine control intensity; uncertainty remains visible as a separate concern."><div className="pv-risk"><div className="pv-risk-matrix"><span className="pv-risk-y">Consequence</span><div className="pv-risk-grid">{Array.from({length:16},(_,i)=><i key={i} data-risk={3-Math.floor(i/4)+i%4}><small>{i===3?'High':i===12?'Low':''}</small></i>)}</div><span className="pv-risk-x">Likelihood</span></div><div className="pv-risk-labels">{labels.map(([action,note],i)=><div key={action}><i data-risk={i*2}/><span><b>{action}</b><small>{note}</small></span></div>)}</div></div></VisualFrame>}
