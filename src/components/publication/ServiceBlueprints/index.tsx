import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function ServiceBlueprintCanvas(props:FamilyProps){const lanes=[['Operator','Observe','Review','Disposition'],['Interface','Brief','Evidence panel','Record'],['Services','Resolve','Assess','Publish'],['Evidence','Source','Context','Outcome']];return <VisualFrame family="service-blueprint" props={props} caption="Frontstage work, backstage services, and evidence systems remain aligned across the decision journey."><div className="pv-blueprint">{lanes.map(([lane,...steps])=><div key={lane}><strong>{lane}</strong>{steps.map(step=><span key={step}>{step}</span>)}</div>)}</div></VisualFrame>}
