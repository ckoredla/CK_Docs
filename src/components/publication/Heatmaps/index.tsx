import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function HeatmapCanvas(props:FamilyProps){const rows=props.tags.length?props.tags.slice(0,3):['Evidence','Context','Authority'];return <VisualFrame family="heatmap" props={props} caption="Concentration and gaps are shown by cohort rather than hidden inside an aggregate average."><div className="pv-heatmap"><div/>{['Fleet A','Fleet B','Fleet C','Fleet D','Fleet E'].map(x=><b key={x}>{x}</b>)}{rows.map((row,r)=><><strong key={row}>{row}</strong>{[0,1,2,3,4].map(c=><i key={`${r}-${c}`} data-level={(r*3+c*2+props.slug.length)%5}/>)}</>)}</div></VisualFrame>}
