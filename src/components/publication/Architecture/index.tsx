import { Archive, Boxes, CloudCog, Database, RadioTower, ShieldCheck, UserCheck, Wrench } from 'lucide-react';
import { VisualFrame, short } from '../Frame'; import type { FamilyProps } from '../types';
export function ArchitectureCanvas(props:FamilyProps){const layers=[{name:'Operational sources',nodes:[[RadioTower,'Aircraft & edge'],[Archive,'Enterprise records']]},{name:'Platform services',nodes:[[CloudCog,'Secure ingestion'],[Database,'Evidence store'],[Boxes,'Context services']]},{name:'Decision plane',nodes:[[ShieldCheck,'Bounded analysis'],[UserCheck,'Qualified review'],[Wrench,'Maintenance action']]}] as const;return <VisualFrame family="architecture" props={props} caption="The reference architecture separates source custody, platform services, analytical boundaries, and accountable maintenance action."><div className="pv-architecture-system"><aside><b>Cross-cutting controls</b><span>Identity</span><span>Lineage</span><span>Observability</span><span>Access</span></aside><div className="pv-architecture-layers">{layers.map((layer,li)=><section key={layer.name}><header><span>0{li+1}</span><b>{layer.name}</b></header><div>{layer.nodes.map(([Icon,label],ni)=><article key={label}><Icon/><b>{label}</b><small>{li===0&&ni===0?short(props.domain):props.tags[(li+ni)%Math.max(1,props.tags.length)]||'governed boundary'}</small>{!(li===2&&ni===2)?<i>→</i>:null}</article>)}</div></section>)}</div></div></VisualFrame>}
