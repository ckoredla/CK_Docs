import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function TableCanvas(props:FamilyProps){const rows=[['Recorded evidence','Source identity','Retain'],['Normalized context','Mapping version','Review'],['Analytical output','Applicability','Bound'],['Operational decision','Qualified role','Record']];return <VisualFrame family="table" props={props} caption="A dense engineering table makes ownership and required controls directly comparable."><div className="pv-table"><div><b>Information class</b><b>Control point</b><b>Treatment</b></div>{rows.map(row=><div key={row[0]}>{row.map(cell=><span key={cell}>{cell}</span>)}</div>)}</div></VisualFrame>}
