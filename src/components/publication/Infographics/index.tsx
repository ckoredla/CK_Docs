import { CircleCheck, CircleDashed, CircleX } from 'lucide-react'; import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function InfographicCanvas(props:FamilyProps){const items=[[CircleCheck,'Recorded fact','Preserve'],[CircleDashed,'Derived signal','Qualify'],[CircleX,'Unsupported claim','Reject']] as const;return <VisualFrame family="infographic" props={props} caption="A compact editorial explainer distinguishes evidence classes and the treatment each class requires."><div className="pv-infographic">{items.map(([Icon,label,action],i)=><div key={label}><span>{i+1}</span><Icon/><b>{label}</b><em>{action}</em></div>)}</div></VisualFrame>}
