import { VisualFrame, numbered } from '../Frame'; import type { FamilyProps } from '../types';
export function TimelineCanvas(props:FamilyProps){const items=['Baseline evidence','Applicability resolved','Work released','Finding reviewed','Outcome recorded'];return <VisualFrame family="timeline" props={props} caption="Milestones expose dependencies, review gates, and the evidence required to advance."><div className="pv-timeline">{numbered(items).map(({item,n},i)=><div key={item} style={{'--span':`${20+i*14}%`} as React.CSSProperties}><span>{n}</span><b>{item}</b><i/></div>)}</div></VisualFrame>}
