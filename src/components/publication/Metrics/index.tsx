import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function MetricsCanvas(props:FamilyProps){return <VisualFrame family="metrics" props={props} caption="Distributions and operating limits are more informative than an isolated average or decorative KPI."><div className="pv-metrics"><div className="pv-metric-curve">{Array.from({length:24},(_,i)=><i key={i} style={{height:`${Math.max(8,92-Math.abs(12-i)*7)}%`}}/>)}<span className="limit">review threshold</span></div><div className="pv-metric-legend"><b>Observed cohort</b><span>median 0.82</span><span>p95 1.27</span><span>n = illustrative</span></div></div></VisualFrame>}
