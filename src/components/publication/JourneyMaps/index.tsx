import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function JourneyMapCanvas(props:FamilyProps){const steps=['Detect','Understand','Coordinate','Act','Learn'];return <VisualFrame family="journey-map" props={props} caption="The reader sees the operational experience, friction, and confidence changes—not only the system transaction path."><div className="pv-journey"><div className="pv-journey-line">{steps.map((step,i)=><div key={step} style={{'--y':`${[55,28,66,38,18][i]}%`} as React.CSSProperties}><i/><b>{step}</b><small>{['signal','context','handoff','authority','outcome'][i]}</small></div>)}</div><blockquote>Critical moment: evidence must remain inspectable at the authority handoff.</blockquote></div></VisualFrame>}
