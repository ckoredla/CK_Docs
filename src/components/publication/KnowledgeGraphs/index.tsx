import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function KnowledgeGraphCanvas(props:FamilyProps){const nodes=['Aircraft','Position','Component','Event','Task','Document','Finding'];return <VisualFrame family="knowledge-graph" props={props} caption="Governed identities and effective-dated relationships connect evidence without turning inferred links into facts."><div className="pv-knowledge">{nodes.map((node,i)=><div key={node} className={`k${i}`}><span>{node}</span><small>{i?['installed at','generated','addresses'][i%3]:'identity root'}</small></div>)}</div></VisualFrame>}
