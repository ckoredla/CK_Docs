import { VisualFrame } from '../Frame'; import type { FamilyProps } from '../types';
export function DecisionTreeCanvas(props:FamilyProps){return <VisualFrame family="decision-tree" props={props} caption="Explicit branches preserve abstention and escalation as valid outcomes when evidence is insufficient."><div className="pv-decision-tree"><div className="root">Evidence applicable?</div><div className="branch yes">Yes</div><div className="branch no">No</div><div className="leaf assess">Assess within boundary</div><div className="leaf repair">Repair context</div><div className="leaf review">Qualified review</div><div className="leaf abstain">Abstain / escalate</div></div></VisualFrame>}
