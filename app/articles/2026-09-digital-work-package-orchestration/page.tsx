import { IssueArticle, issueMetadata } from '../../components/IssueArticle';
const slug='2026-09-digital-work-package-orchestration'; export const metadata=issueMetadata(slug); export default function Page(){return <IssueArticle slug={slug}/>;}
