import { IssueArticle, issueMetadata } from '../../components/IssueArticle';
const slug='2026-12-maintenance-intelligence-operating-system'; export const metadata=issueMetadata(slug); export default function Page(){return <IssueArticle slug={slug}/>;}
