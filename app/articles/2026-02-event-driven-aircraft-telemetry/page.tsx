import { IssueArticle, issueMetadata } from '../../components/IssueArticle';
const slug='2026-02-event-driven-aircraft-telemetry'; export const metadata=issueMetadata(slug); export default function Page(){return <IssueArticle slug={slug}/>;}
