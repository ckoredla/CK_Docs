import { IssueArticle, issueMetadata } from '../../components/IssueArticle';
const slug='2026-04-maintenance-knowledge-retrieval'; export const metadata=issueMetadata(slug); export default function Page(){return <IssueArticle slug={slug}/>;}
