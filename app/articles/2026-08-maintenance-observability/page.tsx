import { IssueArticle, issueMetadata } from '../../components/IssueArticle';
const slug='2026-08-maintenance-observability'; export const metadata=issueMetadata(slug); export default function Page(){return <IssueArticle slug={slug}/>;}
